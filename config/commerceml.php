<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Авторизация обмена 1С
    |--------------------------------------------------------------------------
    */
    'username' => env('COMMERCEML_USERNAME', 'admin'),
    'password' => env('COMMERCEML_PASSWORD', ''),
    'enabled'  => env('COMMERCEML_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | Хранилище файлов обмена
    |--------------------------------------------------------------------------
    */
    'upload_dir'    => storage_path('app/commerceml'),
    'zip_support'   => true,
    'file_limit'    => 100 * 1024 * 1024, // 100 MB
    'image_storage' => 'products', // relative to storage/app/public/

    /*
    |--------------------------------------------------------------------------
    | Маппинг типов цен из 1С
    |--------------------------------------------------------------------------
    | Название типа цены в 1С → поле в БД
    */
    'price_type'     => env('COMMERCEML_PRICE_TYPE', 'Розничная'),
    'old_price_type' => env('COMMERCEML_OLD_PRICE_TYPE', ''),

    /*
    |--------------------------------------------------------------------------
    | Обмен заказами
    |--------------------------------------------------------------------------
    */
    'export_order_statuses' => ['new', 'processing'],

    // Маппинг статусов 1С → сайт
    'order_status_map' => [
        'Принят'   => 'processing',
        'Выполнен' => 'completed',
        'Отменен'  => 'cancelled',
    ],

];
